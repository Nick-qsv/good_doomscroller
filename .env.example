# Local web application
DATABASE_URL=postgresql://doomscroller:doomscroller@localhost:5432/doomscroller
ACTOR_COOKIE_SECRET=replace-with-at-least-32-random-characters
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# First-party session analytics; events require a configured database.
ANALYTICS_ENABLED=true

# Offline book-selection pipeline (optional until model selection is enabled)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
